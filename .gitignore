# macOS
.DS_Store

# SwiftPM / Xcode
.build/
.swiftpm/
DerivedData/
xcuserdata/
*.xcuserstate

# Release artifacts (published via GitHub Releases, never committed)
dist/
*.dmg

# Core ML model artifacts (distributed as Release assets per docs/product-plan.md §4.8)
*.mlpackage
*.mlmodelc

# Third-party QA images fetched by Scripts/qa-benchmark.sh — not ours to mirror
qa/

# Model weights fetched by Scripts/fetch-models.sh — Release assets, never committed
models/weights/

# Core ML conversion spike toolchain — see docs/roadmap.md v0.3
.venv/
models/birefnet_lite_src/
Scripts/__pycache__/
# Weight symlinks inside vendored model-source dirs point at gitignored files
models/*/model.safetensors
